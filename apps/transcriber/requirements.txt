# --- Core Framework ---
# FastAPI: 我們的核心 Web 框架
# uvicorn: ASGI 伺服器，用於運行 FastAPI
# python-multipart: 用於處理表單數據 (檔案上傳)
fastapi
uvicorn
python-multipart

# --- Transcription Engine ---
# 大型轉寫依賴 (如 faster-whisper) 已被移至 requirements.large.txt
# 以便在模擬測試模式下可以跳過安裝。

# --- Utilities ---
# pydantic: FastAPI 使用的數據驗證庫
pydantic
click

# 注意：為了在 'real' 模式下運行，可能需要安裝 PyTorch。
# 我們遵循 TEST.md 的理念，不在這裡直接聲明 torch，
# 而是讓使用者或 CI/CD 環境根據需要自行安裝。
# 這保持了基礎環境的輕量。
#
# 同樣，ffmpeg 也是一個外部依賴，需要系統級的安裝。
