# 開發環境依賴 (包含核心依賴)
-r ../requirements.txt

# --- 測試框架 ---
pytest         # 核心測試框架
pytest-cov     # 測試覆蓋率外掛
pytest-asyncio # 支援異步 (asyncio) 測試
pytest-timeout # 設定測試超時，防止卡死
httpx          # 新一代的 HTTP 客戶端，用於測試 FastAPI

# --- 程式碼品質與預檢工具 ---
ruff           # Linter 和 Formatter，速度極快
deptry         # 依賴關係檢查
mypy           # 靜態型別檢查
bandit         # 程式碼安全掃描
radon          # 程式碼複雜度分析
coverage       # 測試覆蓋率
pre-commit     # Git 提交鉤子管理
pytest-asyncio
duckdb
